class Stream {
    constructor(
            column_index,
            char_index) {
        this.column_index = column_index
        this.row_index = 0
        this.char_index = char_index
    }
}

export default Stream